---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bwapp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "bwapp.fullname" . }}
spec:
  selector:
    matchLabels:
      app: {{ include "bwapp.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "bwapp.fullname" . }}
    spec:
      containers:
        - name: bwapp
          image: neuralegion/beebox
          resources:
            requests:
              cpu: 256m
              memory: 750Mi
            limits:
              cpu: 1
              memory: 1024Mi
              ephemeral-storage: "4Gi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
              scheme: HTTP
            initialDelaySeconds: 120
            periodSeconds: 30

---
kind: Service
apiVersion: v1
metadata:
  name: {{ include "bwapp.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "bwapp.fullname" . }}
  ports:
    - port: 80
    