{{- if .Values.ingress.url -}}
{{- $fullName := include "brokencrystals.fullname" . -}}
{{- $ingressClass := include "brokencrystals.ingressClass" . -}}
{{- $globalAnnotations := .Values.ingress.annotations | default dict -}}

{{/* Main Application Ingress */}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "brokencrystals.labels" . | nindent 4 }}
  annotations:
    {{- if $globalAnnotations }}
    {{- range $key, $value := $globalAnnotations }}
    {{- if eq $ingressClass "nginx" }}
    {{- if hasPrefix "nginx.ingress.kubernetes.io" $key }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- else }}
    {{- if not (hasPrefix "nginx.ingress.kubernetes.io" $key) }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if and .Values.ingress.tls.enabled .Values.ingress.tls.autoGenerate (eq .Values.ingress.cert "") }}
    cert-manager.io/cluster-issuer: letsencrypt-cf-prod
    {{- end }}
spec:
  {{- if $ingressClass }}
  ingressClassName: {{ $ingressClass }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.url }}
      secretName: {{ if eq .Values.ingress.cert "" }}{{ $fullName }}-brokencrystals-secret{{ else }}{{ .Values.ingress.cert }}{{ end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.url }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: 3000

{{/* Keycloak Ingress */}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-keycloak
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "brokencrystals.labels" . | nindent 4 }}
  annotations:
    {{- if $globalAnnotations }}
    {{- range $key, $value := $globalAnnotations }}
    {{- if eq $ingressClass "nginx" }}
    {{- if hasPrefix "nginx.ingress.kubernetes.io" $key }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- else }}
    {{- if not (hasPrefix "nginx.ingress.kubernetes.io" $key) }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if and .Values.ingress.tls.enabled .Values.ingress.tls.autoGenerate (eq .Values.ingress.cert "") }}
    cert-manager.io/cluster-issuer: letsencrypt-cf-prod
    {{- end }}
spec:
  {{- if $ingressClass }}
  ingressClassName: {{ $ingressClass }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ printf "auth%s%s" .Values.ingress.authlevel .Values.ingress.url }}
      secretName: {{ if eq .Values.ingress.cert "" }}{{ $fullName }}-brokencrystals-keycloak-secret{{ else }}{{ .Values.ingress.cert }}{{ end }}
  {{- end }}
  rules:
    - host: {{ printf "auth%s%s" .Values.ingress.authlevel .Values.ingress.url }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-keycloak
                port:
                  number: 8080

{{/* Mailcatcher Ingress */}}
{{- if .Values.useMailcatcher }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-mailcatcher
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "brokencrystals.labels" . | nindent 4 }}
  annotations:
    {{- if $globalAnnotations }}
    {{- range $key, $value := $globalAnnotations }}
    {{- if eq $ingressClass "nginx" }}
    {{- if hasPrefix "nginx.ingress.kubernetes.io" $key }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- else }}
    {{- if not (hasPrefix "nginx.ingress.kubernetes.io" $key) }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if and .Values.ingress.tls.enabled .Values.ingress.tls.autoGenerate (eq .Values.ingress.cert "") }}
    cert-manager.io/cluster-issuer: letsencrypt-cf-prod
    {{- end }}
spec:
  {{- if $ingressClass }}
  ingressClassName: {{ $ingressClass }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ printf "mailcatcher-%s" .Values.ingress.url }}
      secretName: {{ if eq .Values.ingress.cert "" }}{{ $fullName }}-mailcatcher-secret{{ else }}{{ .Values.ingress.cert }}{{ end }}
  {{- end }}
  rules:
    - host: {{ printf "mailcatcher-%s" .Values.ingress.url }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-mailcatcher
                port:
                  number: 1080
{{- end }}
{{- end }}
