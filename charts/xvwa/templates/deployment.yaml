---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "xvwa.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "xvwa.fullname" . }}
spec:
  selector:
    matchLabels:
      app: {{ include "xvwa.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "xvwa.fullname" . }}
    spec:
      containers:
        - name: xvwa
          image: tuxotron/xvwa
          resources:
            requests:
              cpu: 256m
              memory: 750Mi
            limits:
              cpu: 1
              memory: 1024Mi
              ephemeral-storage: "4Gi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
              scheme: HTTP
            initialDelaySeconds: 120
            periodSeconds: 30

---
kind: Service
apiVersion: v1
metadata:
  name: {{ include "xvwa.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "xvwa.fullname" . }}
  ports:
    - port: 80
    